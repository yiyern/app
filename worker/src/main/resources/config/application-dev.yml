server:
  port: 8088

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:~/test
    username: sa
    initialize: true
    schema: classpath:h2/schema.sql
    data: classpath:h2/data.sql
